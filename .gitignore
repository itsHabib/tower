/tower
/tower.exe
*.exe
*.test
*.out

vendor/

.tower/
*.db
*.db-journal
*.db-wal
*.db-shm

.idea/
.vscode/
*.swp
.DS_Store

# integration-test sandbox (created by scripts/setup-test-env.sh)
.sandbox/

# tower's own dev worktrees — created by `tower add <name>` from this
# checkout, not part of the source tree.
.worktrees/

# scratch / handoff prompts only — design docs / features / rfcs are
# tracked source so /ship-feature can discover them via `git log main`.
docs/prompts/
*~
